RewriteEngine On

# If the requested URL is not a directory
RewriteCond %{REQUEST_FILENAME} !-d

# If the requested URL with .html appended is an existing file
RewriteCond %{REQUEST_FILENAME}\.html -f

# Rewrite the URL to serve the .html file without showing the extension
RewriteRule ^(.*)$ $1.html [NC,L]
